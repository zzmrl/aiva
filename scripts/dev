#!/usr/bin/sh

docker compose up -d database

DATABASE_PASSWORD_FILE="./secrets/postgres_password"
DATABASE_USER_FILE="./secrets/postgres_user"
DATABASE_NAME_FILE="./secrets/postgres_user"
DATABASE_HOST="localhost"
DATABASE_PORT="5432"

cd api && bun dev

cd ../webui && bun dev
